// Package llm contains GoDex's provider/model registry and runtime model
// selection strategy types. Conversation clients consume the resolved
// candidates so provider-specific HTTP clients stay behind one normalized
// configuration surface.
package llm
